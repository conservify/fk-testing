#!/bin/bash

set -xe

pushd ~/fieldkit/firmware-common
mkdir -p build
pushd build
cp ../core/config.h.template ../core/config.h
cmake -DDEBUG_UART_FALLBACK=OFF -DDISABLE_RADIO=ON -DWIFI_ALWAYS_ON=ON ../ && make
scp fk-core.bin weather-pi: && ssh weather-pi "./flash-core"
popd
popd
